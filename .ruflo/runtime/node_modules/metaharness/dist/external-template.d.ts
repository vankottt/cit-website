export interface ExternalTemplate {
    manifest: {
        id: string;
        description: string;
    };
    templateRoot: string;
}
export interface PackModule {
    load?(): Promise<ExternalTemplate>;
    default?: {
        load?(): Promise<ExternalTemplate>;
    };
}
/**
 * Resolve an external template package. Returns the loaded template or
 * throws with a clear message.
 */
export declare function loadExternalTemplate(packageName: string): Promise<ExternalTemplate>;
//# sourceMappingURL=external-template.d.ts.map
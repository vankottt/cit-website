export interface Peer {
    id: string;
    name: string;
    endpoint: string;
    trust: 'untrusted' | 'trusted' | 'self';
}
export interface FederationState {
    /** This harness's own peer id. */
    self_id: string;
    /** Peer entries indexed by id. */
    peers: Record<string, Peer>;
    /** ISO-8601 last-updated timestamp. */
    updated_at: string;
}
export declare function emptyState(selfId: string): FederationState;
export declare function loadState(harnessDir: string): Promise<FederationState | null>;
export declare function saveState(harnessDir: string, state: FederationState): Promise<void>;
export declare function addPeer(state: FederationState, peer: Peer): FederationState;
export declare function removePeer(state: FederationState, id: string): FederationState;
export declare function listPeers(state: FederationState, opts?: {
    trusted?: boolean;
}): Peer[];
/**
 * Dispatch the `federate` subcommand.
 *
 * Subactions:
 *   harness federate init <self-id>      — create state
 *   harness federate add <id> <name> <endpoint> [trust]
 *   harness federate remove <id>
 *   harness federate list [--trusted]
 *   harness federate status              — summary
 */
export declare function federateDispatch(args: string[], cwd?: string): Promise<{
    code: number;
    lines: string[];
}>;
//# sourceMappingURL=federate.d.ts.map
type Embedder = {
    embed(text: string): Promise<Float32Array>;
    embedBatch(texts: string[]): Promise<Float32Array[]>;
    /** ADR-090: BGE-en-v1.5 query prefix per BAAI docs. Apply ONLY to
     *  queries (not documents). Measured +0.009 nDCG@10 on NFCorpus
     *  dense-alone (0.352 → 0.360). */
    embedQuery(text: string): Promise<Float32Array>;
    dim(): number;
    modelName(): string;
};
/** BAAI BGE-en-v1.5 (non-icl) query-side prefix per their docs. */
export declare const BGE_QUERY_PREFIX = "Represent this sentence for searching relevant passages: ";
/**
 * Lazy-load the BGE bi-encoder. Defaults to Xenova/bge-base-en-v1.5.
 * Returns null on failure; caller should fall back.
 */
export declare function getBgeEmbedder(modelName?: string): Promise<Embedder | null>;
export declare function getBgeStatus(): {
    loaded: boolean;
    attempted: boolean;
    error: string | null;
    modelName: string | null;
};
export {};
//# sourceMappingURL=bge-embedder.d.ts.map
import { type InstallResult } from './install.js';
export interface EffectiveProxy {
    version: string;
    pid: number;
    executable: string;
}
type Wait = (milliseconds: number) => Promise<void>;
export declare function probeEffectiveProxy(fetcher?: typeof fetch, platform?: NodeJS.Platform): Promise<EffectiveProxy | null>;
export declare function acquireProxyInstallLease(wait?: Wait): Promise<() => void>;
export declare function installAndActivateProxy(version: string, log?: (line: string) => void): Promise<InstallResult & {
    pid: number;
}>;
export {};
//# sourceMappingURL=activation.d.ts.map
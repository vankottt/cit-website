import { a as startHTTPServer, c as getUpstreamBridge, i as DEFAULT_ALLOWED_HEADERS, l as InMemoryEventStore, n as resolveVersionNegotiation, o as proxyServer, r as startStdioServer, s as acquireListenSubscriptions, t as ServerType, u as AuthenticationMiddleware } from "./startStdioServer-BIyBMtB8.mjs";

//#region src/tapTransport.ts
const tapTransport = (transport, eventHandler) => {
	const originalClose = transport.close.bind(transport);
	const originalOnClose = transport.onclose?.bind(transport);
	const originalOnError = transport.onerror?.bind(transport);
	const originalOnMessage = transport.onmessage?.bind(transport);
	const originalSend = transport.send.bind(transport);
	const originalStart = transport.start.bind(transport);
	transport.close = async () => {
		eventHandler({ type: "close" });
		return originalClose?.();
	};
	transport.onclose = async () => {
		eventHandler({ type: "onclose" });
		return originalOnClose?.();
	};
	transport.onerror = async (error) => {
		eventHandler({
			error,
			type: "onerror"
		});
		return originalOnError?.(error);
	};
	transport.onmessage = async (message) => {
		eventHandler({
			message,
			type: "onmessage"
		});
		return originalOnMessage?.(message);
	};
	transport.send = async (message) => {
		eventHandler({
			message,
			type: "send"
		});
		return originalSend?.(message);
	};
	transport.start = async () => {
		eventHandler({ type: "start" });
		return originalStart?.();
	};
	return transport;
};

//#endregion
export { AuthenticationMiddleware, DEFAULT_ALLOWED_HEADERS, InMemoryEventStore, ServerType, acquireListenSubscriptions, getUpstreamBridge, proxyServer, resolveVersionNegotiation, startHTTPServer, startStdioServer, tapTransport };
//# sourceMappingURL=index.mjs.map
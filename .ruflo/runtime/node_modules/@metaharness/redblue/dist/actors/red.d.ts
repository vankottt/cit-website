import type { ModelClient, TestCase } from '../types.js';
/**
 * Mutate a base test's probe into a fresh, same-family probe using a (cheap)
 * model. Returns the original probe unchanged on any failure or empty output.
 */
export declare function mutateProbe(client: ModelClient, model: string, base: TestCase): Promise<{
    probe: string;
    costUsd: number;
}>;
//# sourceMappingURL=red.d.ts.map
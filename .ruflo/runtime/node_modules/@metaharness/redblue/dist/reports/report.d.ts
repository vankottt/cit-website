import type { Gates, RunRates, RunReport, TestResult } from '../types.js';
export interface BuildReportArgs {
    runId: string;
    results: TestResult[];
    rates: RunRates;
    gates: Gates;
    costUsd: number;
    patchReductionRate?: number;
    recommendations?: string[];
}
/** Build the structured JSON report. */
export declare function buildReport(args: BuildReportArgs): RunReport;
/** Render a board-readable markdown summary from a JSON report. */
export declare function renderMarkdown(report: RunReport): string;
//# sourceMappingURL=report.d.ts.map
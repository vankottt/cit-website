import type { AttackFamily, RedBlueConfig } from '../types.js';
declare const ALL_FAMILIES: AttackFamily[];
/** Default config — fully safe, mock target, sensible budgets. */
export declare function defaultConfig(): RedBlueConfig;
type YamlValue = string | number | boolean | YamlValue[] | {
    [k: string]: YamlValue;
};
/**
 * Minimal YAML parser for our flat schema. Handles:
 *   key: value           (scalar / bool / number / string)
 *   key:                  (nested map, indented children)
 *     child: value
 *   key:                  (list)
 *     - item
 * Comments (#) and blank lines are ignored.
 */
export declare function parseYaml(text: string): Record<string, YamlValue>;
/**
 * Build a validated RedBlueConfig from a parsed YAML object (or partial).
 * Applies defaults, then ENFORCES the safety boundary — the returned config
 * is guaranteed to have allow_network/allow_shell/allow_real_credentials off
 * and a validated target.
 */
export declare function buildConfig(raw: Record<string, YamlValue>): RedBlueConfig;
/** Load + validate a config from YAML text. */
export declare function loadConfigFromString(yaml: string): RedBlueConfig;
export { ALL_FAMILIES };
//# sourceMappingURL=loader.d.ts.map
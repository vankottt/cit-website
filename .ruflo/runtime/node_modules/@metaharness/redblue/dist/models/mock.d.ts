import type { ModelClient } from '../types.js';
export type MockResponder = (req: {
    model: string;
    system: string;
    user: string;
}) => string;
export declare class MockModelClient implements ModelClient {
    calls: number;
    readonly costPerCall: number;
    private readonly responder;
    constructor(responder: MockResponder, costPerCall?: number);
    complete(req: {
        model: string;
        system: string;
        user: string;
        maxTokens?: number;
        temperature?: number;
    }): Promise<{
        text: string;
        costUsd: number;
        promptTokens: number;
        completionTokens: number;
    }>;
}
//# sourceMappingURL=mock.d.ts.map
import type { ModelClient } from '../types.js';
/** Build the test-only marker judge. costPerCall defaults to 0 ($0). */
export declare function mockMarkerJudge(costPerCall?: number): ModelClient;
//# sourceMappingURL=mock-judge.d.ts.map
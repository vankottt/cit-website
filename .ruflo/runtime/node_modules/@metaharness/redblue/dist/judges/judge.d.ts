import type { JudgeVerdict, ModelClient, TestCase, TargetTelemetry } from '../types.js';
/** Extract the first balanced JSON object from text (tolerates fences/prose). */
export declare function extractJson(text: string): string | null;
/** Validate + coerce a parsed object into a JudgeVerdict, or null if invalid. */
export declare function validateVerdict(obj: unknown): JudgeVerdict | null;
export declare function parseVerdict(text: string): JudgeVerdict | null;
/**
 * Run the judge over one test outcome. Retries up to `maxRetries` on malformed
 * JSON, then falls back to a conservative verdict (uncompromised, Info) so a
 * flaky judge never produces a false-positive Critical.
 */
export declare function judge(client: ModelClient, model: string, test: TestCase, telemetry: TargetTelemetry, maxRetries?: number): Promise<{
    verdict: JudgeVerdict;
    costUsd: number;
}>;
//# sourceMappingURL=judge.d.ts.map
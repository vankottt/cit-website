import type { Patch, TargetConfig, TargetDriver, TargetTelemetry } from '../types.js';
/** In-proc deterministic target. The fn receives the (guarded) input. */
export declare class MockTargetDriver implements TargetDriver {
    private readonly fn;
    constructor(fn: (input: string) => {
        output: string;
        toolCalls?: string[];
    });
    invoke(input: string): Promise<TargetTelemetry>;
}
/** Drives a loopback/test http endpoint. Safety-validated target only. */
export declare class HttpTargetDriver implements TargetDriver {
    private readonly cfg;
    constructor(cfg: TargetConfig);
    invoke(input: string): Promise<TargetTelemetry>;
}
/**
 * Wrap a driver so blue-team guard rules are enforced. If any guard blocks the
 * request, the target is never invoked and a safe refusal is returned — this is
 * exactly the behavior a real defense would produce.
 */
export declare class PatchedTargetDriver implements TargetDriver {
    private readonly inner;
    private readonly patches;
    constructor(inner: TargetDriver, patches: Patch[]);
    invoke(input: string): Promise<TargetTelemetry>;
}
//# sourceMappingURL=sandbox.d.ts.map
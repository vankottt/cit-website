import type { BeliefStep, CreditConfig, TurnCredit } from './types.js';
/** sign(A_seq) ∈ {−1, 0, +1}. A zero advantage yields zero credits everywhere. */
export declare function outcomeSign(advantage: number): number;
/** The guaranteed ceiling on per-turn advantage modulation, as a fraction of |A_seq|. */
export declare function effectiveBound(config: CreditConfig): number;
/** Turn belief steps into aligned, standardized, bounded per-turn credits. */
export declare function reshapeCredits(steps: BeliefStep[], advantage: number, config: CreditConfig, labels?: Map<number, string>): TurnCredit[];
/** Pivotal turns: |ΔB_k| ≥ pivotalRatio · max_j|ΔB_j|, and strictly > 0. */
export declare function markPivotal(credits: TurnCredit[], pivotalRatio: number): number[];
/** Apply a turn's multiplier to the terminal advantage: Ã_k = A_seq · m_k. */
export declare function reshapedAdvantage(advantage: number, credit: TurnCredit): number;
//# sourceMappingURL=reshape.d.ts.map
/**
 * Explainability Dashboard - CLI-based TUI for transparency and debugging
 *
 * Features:
 * - Real-time trace visualization
 * - Attention heatmaps
 * - Decision tree rendering
 * - Performance profiling
 * - Compliance reports
 */
import { ExecutionTrace } from '../services/explainability-service.js';
export interface DashboardOptions {
    refreshRate?: number;
    theme?: 'light' | 'dark';
    showAttention?: boolean;
    showDecisionTree?: boolean;
    showPerformance?: boolean;
}
export interface DashboardState {
    selectedTraceId: string | null;
    traces: ExecutionTrace[];
    currentView: 'overview' | 'trace' | 'attention' | 'decision' | 'performance' | 'compliance';
}
export declare class ExplainabilityDashboard {
    private service;
    private options;
    private state;
    constructor(options?: DashboardOptions);
    render(): string;
    private renderOverview;
    private renderTraceDetail;
    private renderAttentionView;
    private renderDecisionTreeView;
    private renderDecisionNode;
    private renderPerformanceView;
    private renderComplianceView;
    private renderHeader;
    private renderSection;
    private renderTable;
    private renderHeatmap;
    setSelectedTrace(traceId: string): void;
    setView(view: DashboardState['currentView']): void;
    exportToConsole(): void;
}
//# sourceMappingURL=explainability-dashboard.d.ts.map
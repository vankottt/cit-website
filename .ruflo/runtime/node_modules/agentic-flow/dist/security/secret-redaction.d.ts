/**
 * Secret Redaction Security Module
 * Prevents API key exposure in logs (CVE-2026-005)
 */
export interface RedactionOptions {
    showPrefix?: number;
    showSuffix?: number;
    maskChar?: string;
}
export declare class SecretRedactor {
    private static readonly SECRET_PATTERNS;
    /**
     * Redact API key for safe logging
     */
    redactKey(key: string | undefined, options?: RedactionOptions): string;
    /**
     * Redact secrets from string (for error messages, logs)
     */
    redactString(text: string): string;
    /**
     * Redact secrets from object (deep)
     */
    redactObject<T extends Record<string, any>>(obj: T): T;
    /**
     * Check if a key name suggests it contains a secret
     */
    private isSecretKey;
    /**
     * Create safe environment for child processes
     */
    sanitizeEnvironment(env: NodeJS.ProcessEnv, requiredKeys?: string[]): NodeJS.ProcessEnv;
    /**
     * Log environment variable status without exposing values
     */
    logEnvStatus(keys: string[], env?: NodeJS.ProcessEnv): string[];
}
export declare const secretRedactor: SecretRedactor;
export declare const redactKey: (key: string | undefined, options?: RedactionOptions) => string, redactString: (text: string) => string, redactObject: <T extends Record<string, any>>(obj: T) => T, sanitizeEnvironment: (env: NodeJS.ProcessEnv, requiredKeys?: string[]) => NodeJS.ProcessEnv, logEnvStatus: (keys: string[], env?: NodeJS.ProcessEnv) => string[];
//# sourceMappingURL=secret-redaction.d.ts.map
import { Octokit } from '@octokit/rest';
function resolveOwnerRepo(owner, repo, defaultOwner, defaultRepo) {
    const finalOwner = owner || defaultOwner;
    const finalRepo = repo || defaultRepo;
    if (!finalOwner || !finalRepo) {
        throw new Error('Owner and repo must be specified via constructor config, environment variables (GITHUB_OWNER, GITHUB_REPO), or method parameters.');
    }
    return { owner: finalOwner, repo: finalRepo };
}
export class GitHubService {
    static instance = null;
    octokit;
    defaultOwner;
    defaultRepo;
    constructor(config = {}) {
        const token = config.token || process.env.GITHUB_TOKEN;
        if (!token) {
            console.warn('[GitHubService] No GitHub token provided. Some operations may fail.');
        }
        this.octokit = new Octokit({ auth: token });
        this.defaultOwner = config.owner || process.env.GITHUB_OWNER;
        this.defaultRepo = config.repo || process.env.GITHUB_REPO;
    }
    static getInstance(config) {
        if (!GitHubService.instance) {
            GitHubService.instance = new GitHubService(config);
        }
        return GitHubService.instance;
    }
    static resetInstance() {
        GitHubService.instance = null;
    }
    async createPullRequest(params, owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.pulls.create({
                owner: resolved.owner,
                repo: resolved.repo,
                title: params.title,
                body: params.body,
                head: params.head,
                base: params.base,
                draft: params.draft || false,
            });
            return {
                id: response.data.id,
                number: response.data.number,
                url: response.data.html_url,
                state: response.data.state,
                title: response.data.title,
                created_at: response.data.created_at,
            };
        }
        catch (error) {
            throw new Error(`Failed to create PR: ${error.message}`);
        }
    }
    async getPullRequest(prNumber, owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.pulls.get({
                owner: resolved.owner,
                repo: resolved.repo,
                pull_number: prNumber,
            });
            return {
                id: response.data.id,
                number: response.data.number,
                url: response.data.html_url,
                state: response.data.state,
                title: response.data.title,
                body: response.data.body ?? undefined,
                head: response.data.head.ref,
                base: response.data.base.ref,
                mergeable: response.data.mergeable,
                merged: response.data.merged,
                created_at: response.data.created_at,
                updated_at: response.data.updated_at,
            };
        }
        catch (error) {
            throw new Error(`Failed to get PR #${prNumber}: ${error.message}`);
        }
    }
    async listPullRequests(state = 'open', owner, repo, limit = 100) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.pulls.list({
                owner: resolved.owner,
                repo: resolved.repo,
                state,
                per_page: Math.min(limit, 100),
            });
            return response.data.map(pr => ({
                id: pr.id,
                number: pr.number,
                url: pr.html_url,
                state: pr.state,
                title: pr.title,
                created_at: pr.created_at,
                updated_at: pr.updated_at,
            }));
        }
        catch (error) {
            throw new Error(`Failed to list PRs: ${error.message}`);
        }
    }
    async mergePullRequest(prNumber, mergeMethod = 'merge', owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.pulls.merge({
                owner: resolved.owner,
                repo: resolved.repo,
                pull_number: prNumber,
                merge_method: mergeMethod,
            });
            return {
                merged: response.data.merged,
                sha: response.data.sha,
                message: response.data.message,
            };
        }
        catch (error) {
            throw new Error(`Failed to merge PR #${prNumber}: ${error.message}`);
        }
    }
    async createReview(prNumber, event, body, comments, owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.pulls.createReview({
                owner: resolved.owner,
                repo: resolved.repo,
                pull_number: prNumber,
                event,
                body,
                comments,
            });
            return {
                id: response.data.id,
                state: response.data.state,
                body: response.data.body,
                submitted_at: response.data.submitted_at ?? null,
            };
        }
        catch (error) {
            throw new Error(`Failed to create review on PR #${prNumber}: ${error.message}`);
        }
    }
    async createIssue(params, owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.issues.create({
                owner: resolved.owner,
                repo: resolved.repo,
                title: params.title,
                body: params.body,
                labels: params.labels,
                assignees: params.assignees,
            });
            return {
                id: response.data.id,
                number: response.data.number,
                url: response.data.html_url,
                state: response.data.state,
                title: response.data.title,
                labels: response.data.labels.map((l) => (typeof l === 'string' ? l : l.name ?? '')),
                created_at: response.data.created_at,
            };
        }
        catch (error) {
            throw new Error(`Failed to create issue: ${error.message}`);
        }
    }
    async getIssue(issueNumber, owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.issues.get({
                owner: resolved.owner,
                repo: resolved.repo,
                issue_number: issueNumber,
            });
            return {
                id: response.data.id,
                number: response.data.number,
                url: response.data.html_url,
                state: response.data.state,
                title: response.data.title,
                body: response.data.body ?? undefined,
                labels: response.data.labels.map((l) => (typeof l === 'string' ? l : l.name ?? '')),
                assignees: response.data.assignees?.map((a) => a.login) || [],
                created_at: response.data.created_at,
                updated_at: response.data.updated_at,
            };
        }
        catch (error) {
            throw new Error(`Failed to get issue #${issueNumber}: ${error.message}`);
        }
    }
    async listIssues(state = 'open', labels, owner, repo, limit = 100) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.issues.listForRepo({
                owner: resolved.owner,
                repo: resolved.repo,
                state,
                labels: labels?.join(','),
                per_page: Math.min(limit, 100),
            });
            return response.data
                .filter(issue => !issue.pull_request)
                .map(issue => ({
                id: issue.id,
                number: issue.number,
                url: issue.html_url,
                state: issue.state,
                title: issue.title,
                labels: issue.labels.map((l) => (typeof l === 'string' ? l : l.name ?? '')),
                created_at: issue.created_at,
                updated_at: issue.updated_at,
            }));
        }
        catch (error) {
            throw new Error(`Failed to list issues: ${error.message}`);
        }
    }
    async createRelease(tagName, name, body, draft = false, prerelease = false, owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.repos.createRelease({
                owner: resolved.owner,
                repo: resolved.repo,
                tag_name: tagName,
                name,
                body,
                draft,
                prerelease,
            });
            return {
                id: response.data.id,
                url: response.data.html_url,
                tag_name: response.data.tag_name,
                name: response.data.name,
                draft: response.data.draft,
                prerelease: response.data.prerelease,
                created_at: response.data.created_at,
            };
        }
        catch (error) {
            throw new Error(`Failed to create release: ${error.message}`);
        }
    }
    async getMetrics(owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const [repoResponse, prsResponse] = await Promise.all([
                this.octokit.repos.get({ owner: resolved.owner, repo: resolved.repo }),
                this.octokit.pulls.list({ owner: resolved.owner, repo: resolved.repo, state: 'open', per_page: 1 }),
            ]);
            return {
                stars: repoResponse.data.stargazers_count,
                forks: repoResponse.data.forks_count,
                watchers: repoResponse.data.watchers_count,
                openIssues: repoResponse.data.open_issues_count,
                openPRs: prsResponse.data.length,
                size: repoResponse.data.size,
                language: repoResponse.data.language,
                updated_at: repoResponse.data.updated_at,
            };
        }
        catch (error) {
            throw new Error(`Failed to get metrics: ${error.message}`);
        }
    }
    async getWorkflowRuns(limit = 10, owner, repo) {
        const resolved = resolveOwnerRepo(owner, repo, this.defaultOwner, this.defaultRepo);
        try {
            const response = await this.octokit.actions.listWorkflowRunsForRepo({
                owner: resolved.owner,
                repo: resolved.repo,
                per_page: Math.min(limit, 100),
            });
            return response.data.workflow_runs.map(run => ({
                id: run.id,
                name: run.name ?? '',
                status: run.status,
                conclusion: run.conclusion,
                url: run.html_url,
            }));
        }
        catch (error) {
            throw new Error(`Failed to get workflow runs: ${error.message}`);
        }
    }
}
//# sourceMappingURL=github-service.js.map
/**
 * Path Validation Security Module
 * Prevents path traversal attacks (CVE-2026-004)
 */
export interface PathValidationOptions {
    allowedDir?: string;
    blockedPaths?: string[];
    allowAbsolute?: boolean;
    mustExist?: boolean;
    mustBeFile?: boolean;
    mustBeDirectory?: boolean;
}
export declare class PathValidator {
    private allowedDir;
    private blockedPaths;
    constructor(allowedDir?: string, blockedPaths?: string[]);
    /**
     * Validate a file path against traversal and sensitive path attacks
     */
    validate(filePath: string, options?: PathValidationOptions): string;
    /**
     * Validate multiple paths at once
     */
    validateMany(paths: string[], options?: PathValidationOptions): string[];
    /**
     * Check if a path is safe without throwing
     */
    isSafe(filePath: string, options?: PathValidationOptions): boolean;
}
export declare const pathValidator: PathValidator;
/**
 * Quick validation helper
 */
export declare function validateFilePath(filePath: string, allowedDir?: string, options?: PathValidationOptions): string;
/**
 * Validate path is safe to read
 */
export declare function validateReadPath(filePath: string, allowedDir?: string): string;
/**
 * Validate path is safe to write
 */
export declare function validateWritePath(filePath: string, allowedDir?: string): string;
/**
 * Validate directory path
 */
export declare function validateDirPath(filePath: string, allowedDir?: string): string;
//# sourceMappingURL=path-validator.d.ts.map